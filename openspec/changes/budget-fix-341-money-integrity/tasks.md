# Tasks

Workflow rule: one task group = one GitHub sub-issue (of this change's parent issue) = one PR, merged before the next group starts.

<!-- flow:tracking:start -->
## Tracking

Parent issue: [#341](https://github.com/arutsh/OpenGrantFlow/issues/341) · [Project board](https://github.com/users/arutsh/projects/8)

| Group | Sub-issue | Branch | State |
| --- | --- | --- | --- |
| 1. Exact money storage and arithmetic | [#342](https://github.com/arutsh/OpenGrantFlow/issues/342) | `Budget/fix/Issue-342/money-integrity-group1` | open |
| 2. Atomic budget total tracking — ticket depends on 1 | [#343](https://github.com/arutsh/OpenGrantFlow/issues/343) | `Budget/fix/Issue-343/money-integrity-group2` | open |
| 3. Currency code validation on budget writes — ticket depend… | [#344](https://github.com/arutsh/OpenGrantFlow/issues/344) | `Budget/fix/Issue-344/money-integrity-group3` | open |
| 4. Currency list endpoint and full-list picker — ticket depe… | [#345](https://github.com/arutsh/OpenGrantFlow/issues/345) | `Budget/fix/Issue-345/money-integrity-group4` | open |

_Generated by `scripts/flow.py sync budget-fix-341-money-integrity` — do not edit by hand._
<!-- flow:tracking:end -->

## 1. Exact money storage and arithmetic — Issue #342

- [ ] 1.0 Run `scripts/flow.py start budget-fix-341-money-integrity 1` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 1.1 Add shared `Money` (quantize to 4 dp, `ROUND_HALF_UP`) and `Rate` (10 dp) annotated types in `shared/schemas` that parse to `Decimal` and serialize as JSON numbers (design.md Decisions 1, 3); verify with unit tests that `{"a": 0.1}` parses to `Decimal('0.1')`, `10.123456` rounds to `10.1235`, `412.34567890123456` rounds to `412.3456789012`, and `model_dump_json()` emits a number, not a string
- [ ] 1.2 Switch every money/rate field in `shared/schemas` (budget, budget-with-lines, budget-line, report-line, currency-ledger, plus `LedgerBalance` and the dashboard response amounts) from `float` to `Money`/`Rate`; verify existing schema and route tests pass and response bodies still carry JSON numbers
- [ ] 1.3 Change the 9 model columns to `Numeric(18, 4)` (money) / `Numeric(20, 10)` (`estimated_exchange_rate`) with `Decimal` `Mapped` types, and add the Alembic migration (`USING round(col::numeric, s)`, then recompute `budgets.total_amount` from lines; downgrade casts back to `double precision`) as `000017`; verify `alembic upgrade head` then `downgrade -1` then `upgrade head` succeed against the local Postgres
- [ ] 1.4 Convert FIFO allocation to exact arithmetic: remove `FLOAT_EPSILON` from `currency_conversion_crud.py`/`currency_ledger_services.py`, compare against `Decimal(0)`, type `_consume_fifo`'s amount as `Decimal`; verify with ledger tests for the `budget-currency-ledger` delta scenarios (100.10 split into 33.37 + 66.73 exactly, a 0.0001 remainder recorded as unsatisfied, 0.1 + 0.2 receipts balance to exactly 0.3)
- [ ] 1.5 Replace float literals in SQL aggregates (`func.coalesce(..., 0.0)` → `0` in `dashboard_crud.py` and `budget_crud.py`) and in Python fallbacks (`or 0.0`), and make `_compute_estimated_local_cap` Decimal-safe; verify donor/grantee dashboard tests pass and a result's type is `Decimal`, not `float`
- [ ] 1.6 Convert `excel_export_service.py` accumulators and the remainder check to `Decimal` (`allocated_total`, `FLOAT_EPSILON` removal, `or 0.0` defaults); verify the existing export tests pass unchanged and a LibreOffice headless round-trip still computes the same totals
- [ ] 1.7 Grep `services/budget/app` for remaining float arithmetic on money fields (`0.0`, `float(`, `1e-`) and fix any `Decimal`/`float` mixing; verify a full run of the budget test suite shows no `TypeError` and no SQLAlchemy "Decimal objects natively" warnings beyond the known SQLite one
- [ ] 1.8 Add a Postgres-backed precision check (e2e api suite in `frontend-typescript/e2e` or a dedicated integration test): lines of 0.1 + 0.2 yield `total_amount` exactly 0.3, a 10-dp rate round-trips, and response fields are JSON numbers; verify it passes against the local stack
- [ ] 1.9 Run backend lint/tests clean for `services/budget` (flake8 `--max-line-length=100`) and the chat/ai tests touching shared schemas; PR merged (`Closes` this group's sub-issue)

## 2. Atomic budget total tracking — ticket depends on 1 — Issue #343

- [ ] 2.0 Run `scripts/flow.py start budget-fix-341-money-integrity 2` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 2.1 Add a `commit: bool = True` parameter to `update_budget_line` (the create and delete CRUD already have one), and make create/update/delete in `budget_line_services.py` write the line with `commit=False` and commit once through `recalculate_budget_total` (design.md Decision 5); verify with a test that forces the recalculation commit to fail and asserts neither the line change nor a new total persisted
- [ ] 2.2 Log a warning in `recalculate_budget_total` when the budget is not found; verify with a `caplog` test
- [ ] 2.3 Move `total_amount` off `BudgetBase` onto the response schemas and add a before-mode check on `BudgetCreate` and `CreateBudgetWithLinesRequest` that rejects the key's presence (design.md Decision 6); verify create, update, and create-with-lines requests carrying `total_amount` return 422 and write nothing, while normal requests and responses are unchanged
- [ ] 2.4 Remove the now-dead `total_amount` `hide=True` entries from chat's `mcp_bridge.py`; verify chat's MCP bridge tests pass and the tool schemas no longer list `total_amount`
- [ ] 2.5 Run backend lint/tests clean for `services/budget` and `services/chat`; PR merged (`Closes` this group's sub-issue)

## 3. Currency code validation on budget writes — ticket depends on 2 — Issue #344

- [ ] 3.0 Run `scripts/flow.py start budget-fix-341-money-integrity 3` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 3.1 Add a `field_validator` for `local_currency`/`actual_currency` on `BudgetCreate` and `CreateBudgetWithLinesRequest` only (not `BudgetBase`), uppercasing and rejecting non-ISO-4217 codes via `validate_currency`, following `admin_management_schema.py` (design.md Decision 7); verify the `currency-code-validation` scenarios: `XYZ` on create, `EURO` on update, and `ABC` on create-with-lines each return 422 and write nothing, `kes` stores as `KES`, and an omitted/null currency passes
- [ ] 3.2 Verify a budget seeded with a legacy invalid code is still returned by the single-budget and list endpoints (the "Legacy invalid code on read" scenario)
- [ ] 3.3 Verify chat's Excel-import path surfaces the 422 for an invalid extracted currency rather than a 500; adjust chat's error mapping if it doesn't
- [ ] 3.4 Run backend lint/tests clean for `services/budget` and `services/chat`; PR merged (`Closes` this group's sub-issue)

## 4. Currency list endpoint and full-list picker — ticket depends on 3 — Issue #345

- [ ] 4.0 Run `scripts/flow.py start budget-fix-341-money-integrity 4` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 4.1 Add authenticated `GET /budgets/currencies` returning `{code, name}` entries sorted by code from `currency_service`, registered before `GET /budgets/{budget_id}` (design.md Decision 8); verify with route tests: 200 with `AMD`/`JPY` present, 401 unauthenticated, and every returned code passes `validate_currency`
- [ ] 4.2 Confirm the route is reachable through all three gateway configs (`nginx-dev.conf`, `nginx.conf`, `Caddyfile`) under the existing `/api/v1/budgets/` prefix, adding wiring only if the prefix doesn't already cover it; verify with a request through the dev gateway
- [ ] 4.3 Replace the hardcoded `CURRENCY_CODES` in `frontend-typescript/src/utils/currency.ts` with a React Query hook on the new endpoint and use it in every budget currency picker; verify with a component test that the picker renders codes outside the old 11-code list
- [ ] 4.4 Run frontend lint/tests and backend lint/tests for `services/budget` clean; manually verify end to end: create a budget in a currency outside the old list, add fractional lines (0.1 + 0.2) and check the displayed total, record a receipt and conversion, and confirm an Excel export opens with correct totals; PR merged (`Closes` this group's sub-issue)
