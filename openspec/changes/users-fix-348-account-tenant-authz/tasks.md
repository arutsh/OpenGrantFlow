# Tasks

Workflow rule: one task group = one GitHub sub-issue (of this change's parent issue) = one PR, merged before the next group starts.

<!-- flow:tracking:start -->
## Tracking

Parent issue: [#348](https://github.com/arutsh/OpenGrantFlow/issues/348) · [Project board](https://github.com/users/arutsh/projects/8)

| Group | Sub-issue | Branch | State |
| --- | --- | --- | --- |
| 1. Close anonymous privileged account creation | [#349](https://github.com/arutsh/OpenGrantFlow/issues/349) | `Users/fix/Issue-349/account-tenant-authz-group1` | open |
| 2. Self-service-only profile updates — ticket depends on 1 | [#350](https://github.com/arutsh/OpenGrantFlow/issues/350) | `Users/fix/Issue-350/account-tenant-authz-group2` | open |
| 3. Impersonation-scoped company deactivation — ticket depend… | [#351](https://github.com/arutsh/OpenGrantFlow/issues/351) | `Users/fix/Issue-351/account-tenant-authz-group3` | open |

_Generated by `scripts/flow.py sync users-fix-348-account-tenant-authz` — do not edit by hand._
<!-- flow:tracking:end -->

## 1. Close anonymous privileged account creation — Issue #349

- [ ] 1.0 Run `scripts/flow.py start users-fix-account-tenant-authz 1` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 1.1 Remove `create_user_endpoint` (`POST /api/users/`) and any now-unused imports/schemas (`UserCreate` if unreferenced); grep services, tests, `frontend-typescript/e2e`, and `scripts/` for callers and migrate them; verify `POST /api/users/` returns 404/405 in a route test.
- [ ] 1.2 Remove `customer_id` from `RegisterRequest` and stop passing it in `register_endpoint`; verify a test registering with another company's `customer_id` yields a user with `customer_id IS NULL`.
- [ ] 1.3 Add an end-to-end regression test: register (with `role`/`customer_id` extras) → resend-verification → verify-email, asserting the decoded access token has `role == "user"` and no `customer_id`.
- [ ] 1.4 Add a test that `POST /api/users/` with `role: superuser, status: active` creates no row.
- [ ] 1.5 Draft a read-only query that lists `role='superuser'` users and `status='pending'` users with a non-null `customer_id` created before this deploy, and hand it to the user to run on prod (don't run it yourself). Record the outcome in the PR.
- [ ] 1.6 Run `pytest services/users` and `flake8 --max-line-length=100` clean; PR merged.

## 2. Self-service-only profile updates — ticket depends on 1 — Issue #350

- [ ] 2.0 Run `scripts/flow.py start users-fix-account-tenant-authz 2` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 2.1 Add `UserSelfUpdate` (`first_name`, `last_name`, `new_customer_name`, `extra="forbid"`) and switch `PATCH /users/{user_id}/` to it; verify that bodies containing `customer_id`, `role`, `status` or `email` get 422.
- [ ] 2.2 Rewrite `update_user_endpoint` as self-only (`403` when token `user_id` ≠ path id, including for superuser and impersonation tokens), writing only fields the caller sent; keep the pending-founder `new_customer_name` path; verify with tests for each scenario in `specs/company-onboarding/spec.md`.
- [ ] 2.3 Regression test: an admin of A PATCHes only `first_name` → `customer_id` and `role` unchanged (the old `customer_id = None` wipe).
- [ ] 2.4 Regression test: an admin of A PATCHes `customer_id=B` → 422, still admin of A; also a pending user PATCHing `customer_id=B` → 422, no membership.
- [ ] 2.5 Remove `UserUpdate` and `is_superuser(db, …)` from `user_crud.py` if they have no remaining references; verify the frontend onboarding flow still works (`npm test` for onboarding plus a manual onboarding on local dev).
- [ ] 2.6 Run `pytest services/users` and `flake8 --max-line-length=100` clean; PR merged.

## 3. Impersonation-scoped company deactivation — ticket depends on 2 — Issue #351

- [ ] 3.0 Run `scripts/flow.py start users-fix-account-tenant-authz 3` to move this group to In Progress and create its branch before starting any other work in this group.
- [ ] 3.1 Change `deactivate_company_service` so an impersonation token must match the target `customer_id` and a non-impersonating caller must carry `role == "superuser"`; verify with tests for all scenarios in `specs/superuser-tenant-administration/spec.md`.
- [ ] 3.2 Add a test that an impersonation token for A gets 403 on every users-service write that targets B (`/users/{id}/role`, `/users/{id}/remove`, `PATCH /customers/{id}`, `/customers/{id}/deactivate`, `PATCH /users/{id}/`).
- [ ] 3.3 Verify that the real superuser `user_id` is recorded in the `privileged_access_logs` row for an impersonated deactivation.
- [ ] 3.4 Run `pytest services/users` and `flake8 --max-line-length=100` clean; PR merged.
